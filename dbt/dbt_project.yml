name: financial_etl_reliability_lab
version: 1.0.0
config-version: 2
profile: financial_etl_reliability_lab

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  financial_etl_reliability_lab:
    staging:
      +materialized: view
    marts:
      +materialized: table
